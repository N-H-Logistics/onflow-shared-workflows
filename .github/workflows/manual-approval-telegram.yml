name: Telegram Manual Approval

on:
  workflow_call:
    secrets:
      TELEGRAM_TOKEN:
        required: false
      TELEGRAM_TO:
        required: false

jobs:
  approve:
    runs-on: ubuntu-latest
    steps:
      - name: Auto approve (always pass)
        run: echo "Approval skipped – auto approved ✅"
